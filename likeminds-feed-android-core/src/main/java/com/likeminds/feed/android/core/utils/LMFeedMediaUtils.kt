package com.likeminds.feed.android.core.utils

object LMFeedMediaUtils {
    const val DEFAULT_DOCUMENTS_LIMIT = 10
    const val DEFAULT_DOCUMENTS_SIZE_LIMIT = 100L //in MegaBytes
    const val DEFAULT_VIDEO_SIZE_LIMIT = 100L //in MegaBytes
    const val DEFAULT_MEDIA_LIMIT = 10
}